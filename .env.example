# NAS Connection (configured via Settings UI)
# NAS_ADDRESS=192.168.1.100
# NAS_PROTOCOL=smb
# NAS_SHARE=volume1/music
# NAS_USERNAME=admin
# NAS_PASSWORD=
# NAS_PORT=445

# AI Model API Keys (configured via Settings UI)
# These are stored in the database, not in .env
# See Settings > AI Model to configure
