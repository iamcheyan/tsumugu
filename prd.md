# PRD: NAS 文件浏览器 + YouTube 音频下载器

## 1. 介绍

这是一个面向个人 NAS 使用场景的 Web 工具，核心功能是文件浏览器 + YouTube 音频下载器。技术栈为 Python/FastAPI/Jinja2/HTMX/SQLite/yt-dlp/ffmpeg/Docker。产品定位非常明确：极简、单用户、无需登录和权限，专注于浏览目录和快速下载 YouTube 音频（支持自动拆分合集）。

整个产品的核心体验应该是：浏览 NAS 目录 → 进入目标文件夹 → 粘贴 YouTube 链接 → 自动下载 → 自动转音频 → 自动拆分歌曲 → 文件立即出现在当前目录，让收集音乐、播客、演讲、学习资料变得极其简单。

## 2. 目标

### 2.1 产品目标
- 提供一个轻量级的 NAS 文件浏览器，支持基本的文件操作
- 集成 YouTube 音频下载功能，支持一键下载并转换格式
- 支持音频自动拆分，特别针对 YouTube 音乐合集视频
- 提供简洁高效的用户体验，减少操作步骤

### 2.2 技术目标
- 使用现代化技术栈（FastAPI + HTMX）实现高响应性界面
- 通过 Docker 容器化部署，简化安装和配置
- 使用 SQLite 存储配置和下载历史，无需外部数据库
- 实现跨平台兼容性，支持主流操作系统

## 3. 用户故事

### 3.1 文件浏览功能
1. **作为用户，我希望能看到 NAS 目录的树状结构**，以便快速导航到目标文件夹
2. **作为用户，我希望能查看文件列表（文件名、大小、修改时间、类型）**，以便了解当前目录内容
3. **作为用户，我希望能搜索文件**，以便快速找到特定文件
4. **作为用户，我希望能按不同字段排序文件列表**，以便更好地组织视图
5. **作为用户，我希望能创建新文件夹**，以便整理下载的文件
6. **作为用户，我希望能重命名文件**，以便更好地管理文件
7. **作为用户，我希望能删除文件（默认回收站）**，以便清理不需要的文件
8. **作为用户，我希望能移动文件**，以便重新组织文件结构

### 3.2 YouTube 下载功能
1. **作为用户，我希望能在顶部看到固定的下载栏**，以便随时开始新的下载任务
2. **作为用户，我希望能输入 YouTube URL 并选择音频格式（MP3/M4A/FLAC）**，以便下载所需格式的音频
3. **作为用户，我希望系统能自动获取视频信息（封面、标题、时长）**，以便确认下载内容
4. **作为用户，我希望看到实时下载进度**，以便了解下载状态
5. **作为用户，我希望能支持多个下载任务排队**，以便批量下载
6. **作为用户，我希望下载完成后文件自动出现在当前目录**，以便立即访问

### 3.3 音频拆分功能
1. **作为用户，我希望能选择是否启用自动拆分歌曲**，以便处理 YouTube 音乐合集
2. **作为用户，我希望能选择拆分模式（章节信息/静音检测）**，以便根据视频特点选择最佳方式
3. **作为用户，我希望能选择是否保留原始文件**，以便保留完整下载
4. **作为用户，我希望拆分后的文件能自动使用章节标题命名**，以便获得有意义的文件名

### 3.4 音频播放功能
1. **作为用户，我希望能点击音频文件时弹出轻量播放器**，以便预览下载的音频
2. **作为用户，我希望播放器支持基本控制（播放/暂停/进度拖动）**，以便控制播放
3. **作为用户，我希望能看到播放时长**，以便了解音频长度

### 3.5 系统配置
1. **作为用户，我希望能通过 Web UI 设置 NAS 根目录**，以便指定要浏览的目录
2. **作为用户，我希望能配置文件删除策略（回收站/直接删除）**，以便根据需要选择
3. **作为用户，我希望系统启动时自动检测 yt-dlp 和 ffmpeg**，以便确保依赖可用

## 4. 功能需求

### 4.1 核心功能
1. **目录树导航**
   - 显示 NAS 挂载目录的树状结构
   - 支持展开/折叠目录节点
   - 显示当前选中目录
   - 支持返回上级目录

2. **文件列表**
   - 显示文件名、文件大小、修改时间、文件类型
   - 支持文件类型图标显示
   - 支持多选文件
   - 支持文件预览（音频文件）

3. **文件操作**
   - 浏览目录：双击进入目录
   - 返回上级目录：点击返回按钮
   - 搜索：支持文件名搜索
   - 排序：支持按名称、大小、修改时间、类型排序
   - 重命名：右键菜单或快捷键重命名
   - 删除：支持回收站模式（默认）或直接删除
   - 新建文件夹：创建新目录
   - 移动文件：支持拖拽或剪切/粘贴

4. **YouTube 下载**
   - 输入框：YouTube URL 输入
   - 格式选择：下拉菜单选择 MP3/M4A/FLAC
   - 自动拆分选项：复选框启用/禁用
   - 拆分模式选择：章节信息/静音检测
   - 保留原始文件选项：复选框
   - 保存位置：自动使用当前浏览目录
   - 下载按钮：开始下载任务

5. **下载状态**
   - 实时进度条显示
   - 显示视频标题、当前进度、剩余时间
   - 支持任务排队和并发下载
   - 下载完成自动刷新文件列表

6. **音频播放器**
   - 右侧弹出轻量播放器
   - 支持播放/暂停控制
   - 支持进度拖动
   - 显示播放时长和当前时间

### 4.2 界面设计
1. **布局**
   - 左侧：目录树导航（固定宽度）
   - 中间：文件列表区域（主要区域）
   - 顶部：固定下载栏
   - 右侧：音频播放器（可隐藏）

2. **设计风格**
   - 极简风格，深色主题优先
   - 类似 Finder、FileBrowser、VS Code Explorer
   - 优先考虑功能效率
   - 尽量减少点击次数

3. **响应式设计**
   - 支持不同屏幕尺寸
   - 移动端适配（基本功能）

### 4.3 系统功能
1. **配置管理**
   - NAS 根目录配置（通过 Web UI 设置）
   - 文件删除策略配置（回收站/直接删除）
   - 下载目录配置（默认为当前浏览目录）

2. **依赖检测**
   - 启动时自动检测 yt-dlp 是否安装
   - 启动时自动检测 ffmpeg 是否安装
   - 缺失依赖时显示安装提示

3. **数据存储**
   - SQLite 数据库存储：
     - 系统配置
     - 下载任务历史
     - 文件元数据缓存
     - 用户偏好设置

## 5. 非目标

### 5.1 明确不包含的功能
1. **多用户支持**：不考虑多用户、登录、权限管理
2. **社交功能**：不添加分享、评论、点赞等社交功能
3. **推荐系统**：不添加内容推荐、AI 推荐等功能
4. **AI 功能**：不添加 AI 聊天、智能分类等人工智能功能
5. **媒体服务器**：不添加 Plex、Jellyfin、Emby 那种复杂媒体库功能
6. **视频下载**：只支持音频下载，不支持视频文件下载
7. **字幕下载**：不支持字幕文件下载
8. **播放列表管理**：不支持 YouTube 播放列表管理
9. **批量下载**：不支持从播放列表批量下载（只支持单个视频）

### 5.2 设计边界
1. **不追求完全兼容性**：主要针对现代浏览器（Chrome、Firefox、Safari、Edge）
2. **不追求极致性能**：个人使用场景，不需要处理高并发
3. **不追求完美 UI**：功能优先，界面简洁即可
4. **不追求自动化测试**：个人项目，手动测试为主

## 6. 技术考虑

### 6.1 技术栈
- **后端**：Python 3.10+，FastAPI，Jinja2 模板引擎
- **前端**：HTMX（动态交互），HTML/CSS/JavaScript
- **数据库**：SQLite（轻量级，无需额外配置）
- **下载工具**：yt-dlp（YouTube 下载），ffmpeg（音频转换）
- **部署**：Docker 容器化

### 6.2 架构设计
1. **前后端分离**
   - FastAPI 提供 REST API
   - Jinja2 渲染初始页面
   - HTMX 处理动态更新

2. **模块化设计**
   - 文件管理模块
   - YouTube 下载模块
   - 音频处理模块
   - 配置管理模块

3. **异步处理**
   - 使用 FastAPI 的异步特性
   - 下载任务使用后台任务处理
   - 实时进度通过 WebSocket 或 SSE 推送

### 6.3 数据流
1. **文件操作流程**
   ```
   用户操作 → HTMX 请求 → FastAPI 处理 → 文件系统操作 → 返回结果 → 前端更新
   ```

2. **下载流程**
   ```
   用户输入 URL → 获取视频信息 → 显示预览 → 用户确认 → 开始下载 → 转换格式 → 保存文件 → 刷新列表
   ```

3. **拆分流程**
   ```
   下载完成 → 分析章节信息/静音检测 → 切割音频 → 重命名文件 → 保存到目录
   ```

### 6.4 安全性考虑
1. **文件操作安全**
   - 限制目录遍历，防止访问系统文件
   - 文件名清理，防止路径注入
   - 删除操作确认，防止误删

2. **下载安全**
   - URL 验证，只允许 YouTube 链接
   - 文件大小限制，防止恶意下载
   - 下载目录限制，只允许在配置的目录内

### 6.5 性能考虑
1. **文件列表性能**
   - 使用分页加载大目录
   - 文件元数据缓存
   - 异步文件操作

2. **下载性能**
   - 支持多任务并发下载
   - 进度实时更新
   - 下载限速可选

## 7. 开放问题

### 7.1 待确认问题
1. **音频拆分精度**：静音检测的阈值应该如何设置？是否需要用户可调节？
2. **文件预览**：除了音频文件，是否需要支持其他文件类型的预览（如图片、视频缩略图）？
3. **批量操作**：是否需要支持批量文件操作（批量移动、批量删除）？
4. **文件搜索范围**：搜索是否应该支持子目录递归搜索？
5. **下载历史**：是否需要保留完整的下载历史记录？如何清理历史？
6. **错误处理**：下载失败时应该如何处理？是否需要重试机制？
7. **多语言支持**：是否需要支持多语言界面？
8. **移动端优化**：移动端需要支持到什么程度？

### 7.2 技术决策
1. **WebSocket vs SSE**：实时进度推送使用 WebSocket 还是 Server-Sent Events？
2. **前端状态管理**：HTMX 是否足够，还是需要额外的 JavaScript 状态管理？
3. **文件缓存策略**：文件元数据缓存多久更新一次？
4. **日志系统**：是否需要详细的日志记录？如何存储日志？

## 8. 验收标准

### 8.1 功能验收
1. **文件浏览**：能够正常浏览 NAS 目录，显示文件信息
2. **文件操作**：能够完成所有基本文件操作（创建、重命名、删除、移动）
3. **YouTube 下载**：能够下载 YouTube 视频并转换为音频
4. **音频拆分**：能够自动拆分合集视频为单曲文件
5. **音频播放**：能够播放下载的音频文件
6. **配置管理**：能够通过 Web UI 配置系统参数

### 8.2 性能验收
1. **响应时间**：文件列表加载 < 2秒（1000个文件以内）
2. **下载速度**：支持满带宽下载
3. **内存使用**：正常运行时内存占用 < 500MB
4. **并发支持**：支持 5个并发下载任务

### 8.3 稳定性验收
1. **无崩溃**：连续运行 24小时无崩溃
2. **错误恢复**：下载失败能够正确处理，不影响其他功能
3. **数据安全**：文件操作不会导致数据丢失
4. **兼容性**：支持 Chrome、Firefox、Safari、Edge 最新版本

## 9. 里程碑计划

### 9.1 第一阶段：基础框架（1-2周）
- 项目初始化和技术栈搭建
- 基础文件浏览功能
- 简单的界面布局

### 9.2 第二阶段：核心功能（2-3周）
- YouTube 下载功能
- 音频转换和格式选择
- 下载状态显示

### 9.3 第三阶段：高级功能（1-2周）
- 音频自动拆分功能
- 音频播放器
- 配置管理界面

### 9.4 第四阶段：优化完善（1周）
- 界面优化和交互改进
- 错误处理和边界情况
- 性能优化和测试

## 10. 风险评估

### 10.1 技术风险
1. **yt-dlp 稳定性**：YouTube 可能更改 API，导致下载失败
2. **音频拆分准确性**：静音检测可能不够准确
3. **大文件处理**：处理大音频文件时可能的内存问题

### 10.2 产品风险
1. **功能蔓延**：可能无意中添加过多功能，违背极简原则
2. **用户体验**：界面可能过于简单，影响使用效率
3. **维护成本**：个人项目可能缺乏持续维护

### 10.3 缓解措施
1. **定期更新**：保持 yt-dlp 和 ffmpeg 版本更新
2. **用户反馈**：根据实际使用情况调整功能
3. **模块化设计**：保持代码模块化，便于维护和扩展